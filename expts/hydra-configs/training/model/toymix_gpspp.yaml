# @package _global_

constants:
  name: neurips2023_small_data_gpspp
  wandb:
    name: ${constants.name}
    project: neurips2023-expts
    entity: multitask-gnn
  seed: 42
  max_epochs: 100
  data_dir: expts/data/neurips2023/small-dataset
  raise_train_error: true
  datacache_path: ../datacache/neurips2023-small/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/neurips2023-small-gpspp/${now:%Y-%m-%d_%H-%M-%S}/