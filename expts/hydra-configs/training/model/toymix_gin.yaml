# @package _global_

constants:
  name: neurips2023_small_data_gin
  wandb:
    name: ${constants.name}
    project: neurips2023-expts
    entity: multitask-gnn
  seed: 42
  max_epochs: 100
  data_dir:  ../data/graphium/small-dataset/
  raise_train_error: true
  datacache_path: ../datacache/small-dataset/

trainer:
  model_checkpoint:
    dirpath: model_checkpoints/neurips2023-small-gin/${now:%Y-%m-%d_%H-%M-%S}/